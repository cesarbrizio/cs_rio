import type { MapEntityKind, MapStructureKind, MapStructureDefinition } from '@cs-rio/shared';
const x: MapEntityKind = 'market';
const y: MapStructureKind = 'factory';
let z: MapStructureDefinition | null = null;
void x; void y; void z;
